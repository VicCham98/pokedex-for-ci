name: Say hello

on:
  workflow_dispatch:
    inputs:
      name:
        type: choice
        description: Who to greet
        required: true
        options:
          - monalisa
          - cschleiden

jobs:
  hello_world:
    runs-on: ubuntu-18.04
    steps:
      - name: Echo the message
        run: |
          echo "Hola midu!"
      - name: Dime la fecha
        run: date
      - run: ls -l